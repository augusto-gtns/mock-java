# general
spring.profiles.active=@activatedProperties@
spring.devtools.add-properties=false
#server.servlet.context-path=/context
#debug=true

# logging levels
sin.log.level=INFO
logging.level.org.springframework=${sin.log.level}

# database
mock.db.source=localhost:3306/sin_dev
#spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=jdbc:mysql://${mock.db.source}?useTimezone=true&serverTimezone=GMT-3
spring.datasource.username=gtns
spring.datasource.password=gtns
spring.jpa.database-platform = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# custom properties
sin.timezone=America/Sao_Paulo

# ports
server.port=443
# custom port
mock.api.http.port=80
mock.api.http.interface=0.0.0.0

# actuator
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=health

# security
spring.security.user.name=api-user
spring.security.user.password=pass

# swagger
#mock.api.swagger.basepath=/api